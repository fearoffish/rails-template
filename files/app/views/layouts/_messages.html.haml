/ Rails flash messages styled for Zurb Foundation
- flash.each do |name, msg|
  - if msg.is_a?(String) && msg.length > 0
    %div{ "data-alert" => "", "class" => "alert-box round #{ name == :notice ? "success" : "alert" }"}
      = content_tag :div, msg
      %a.close{:href => "#"} &times;